import { TipoGrupoPipe } from './tipo-grupo.pipe';

describe('TipoGrupoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoGrupoPipe();
    expect(pipe).toBeTruthy();
  });
});
